name: hello world
on: push
jobs:
  my-job:
     runs-on: ubuntu-latest
     steps:
     - name: my-step
       run: echo "hello world"
  second:
   needs: my-job
   runs-on: ubuntu-latest
   steps:
     - name: step2
       run: echo "secoond job"
